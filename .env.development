# API locale (compte admin créé via: python manage.py ensure_admin --reset)
VITE_API_BASE_URL=http://127.0.0.1:8000/api
# Production Render (décommenter après: migrate + ensure_admin --reset sur Render)
# VITE_API_BASE_URL=https://flotte-api.onrender.com/api